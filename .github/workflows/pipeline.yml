name: pipeline

on:
  pull_request:
    branches:
      - master

jobs:
  cypress-ga-1-cache:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node: [10, 12]
    name: Cypress npm node v${{ matrix.node }}
    steps:
        - uses: actions/checkout@v1
        - uses: actions/setup-node@v1
          with:
            node-version: ${{ matrix.node }}
            registry-url: https://registry.npmjs.org/
        - uses: actions/cache@v2
          env:
            cache-name: cache-node-modules
          with:
            path: ~/.npm
            key: ${{ runner.os }}-build-${{ env.cache-name }}-${{ hashFiles('**/package-lock.json') }}
            restore-keys: |
              ${{ runner.os }}-build-${{ env.cache-name }}-
              ${{ runner.os }}-build-
              ${{ runner.os }}-
        - run: npm ci
        - uses: cypress-io/github-action@v2
          with:
            install: false
            browser: chrome
            config-file: ./cypress.json
  
  cypress-ga-1-no-cache:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node: [10, 12]
    name: Cypress GA node v${{ matrix.node }}
    steps:
        - uses: actions/checkout@v1
        - uses: actions/setup-node@v1
          with:
            node-version: ${{ matrix.node }}
            registry-url: https://registry.npmjs.org/
        - uses: cypress-io/github-action@v2
          with:
            browser: chrome
            config-file: ./cypress.json
  
  # cypress-ga-3:
  #   runs-on: ubuntu-latest
  #   strategy:
  #     matrix:
  #       cypress-config: ["a", "c"]
  #   name: Cypress GA 2
  #   steps:
  #       - uses: actions/checkout@v1
  #       - uses: actions/setup-node@v1
  #         with:
  #           node-version: 12
  #           registry-url: https://registry.npmjs.org/
  #       - uses: cypress-io/github-action@v2
  #         with:
  #           browser: chrome
  #           config-file: ./cypress-${{ matrix.cypress-config }}.json
